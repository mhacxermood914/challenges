/* eslint-disable linebreak-style */
/* eslint-disable import/no-unresolved */
/* eslint-disable import/prefer-default-export */
import { IRoles } from 'interfaces';

type role = [IRoles, IRoles, IRoles, IRoles, IRoles]

export default {}